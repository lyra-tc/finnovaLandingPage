# SSG + SEO para Finnova — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pre-renderizar la landing de Finnova con `vite-ssg` para que Google indexe HTML real, y agregar meta tags SEO completos con `@vueuse/head`.

**Architecture:** `vite-ssg` reemplaza `createApp` por `ViteSSG`, generando `dist/index.html` con HTML estático en build time. `@vueuse/head` inyecta `<title>`, Open Graph y Twitter Card en ese HTML. Vercel sigue llamando `npm run build` y sirviendo `dist/` sin cambios en su config.

**Tech Stack:** Vue 3, Vite 7, vite-ssg, @vueuse/head, vue-i18n, Tailwind CSS 4, Vercel

---

## File Map

| Acción | Archivo |
|--------|---------|
| Modificar | `frontend/package.json` |
| Modificar | `frontend/vite.config.js` |
| Modificar | `frontend/src/main.js` |
| Modificar | `frontend/src/App.vue` |
| Modificar | `frontend/index.html` |
| Crear | `frontend/src/composables/useSEO.js` |
| Crear | `frontend/public/robots.txt` |
| Crear | `frontend/public/sitemap.xml` |

---

### Task 1: Instalar dependencias

**Files:**
- Modify: `frontend/package.json`

- [ ] **Step 1: Instalar desde `frontend/`**

```bash
cd frontend
npm install vite-ssg @vueuse/head
```

Salida esperada: `added N packages` sin errores. Si hay un peer dep warning sobre Vite 7, ejecutar con `--legacy-peer-deps`.

- [ ] **Step 2: Verificar que aparecen en `package.json`**

Confirmar que `dependencies` en `frontend/package.json` incluye:
```json
"vite-ssg": "...",
"@vueuse/head": "..."
```

- [ ] **Step 3: Commit**

```bash
git add frontend/package.json frontend/package-lock.json
git commit -m "feat(seo): install vite-ssg and @vueuse/head"
```

---

### Task 2: Actualizar scripts en `package.json`

**Files:**
- Modify: `frontend/package.json`

- [ ] **Step 1: Cambiar el script `"build"`**

En `frontend/package.json`, reemplazar el bloque `"scripts"` con:

```json
"scripts": {
  "dev": "vite",
  "build": "vite-ssg build",
  "build:preview": "vite build",
  "preview": "vite preview",
  "format": "prettier --write ."
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/package.json
git commit -m "feat(seo): switch build script to vite-ssg"
```

---

### Task 3: Actualizar `vite.config.js`

**Files:**
- Modify: `frontend/vite.config.js`

- [ ] **Step 1: Agregar `ssgOptions` al config existente**

Reemplazar el contenido completo de `frontend/vite.config.js` con:

```js
import { fileURLToPath, URL } from 'node:url';
import { defineConfig, loadEnv } from 'vite';
import vue from '@vitejs/plugin-vue';
import vueDevTools from 'vite-plugin-vue-devtools';

export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, process.cwd(), 'VITE_');

  return {
    plugins: [vue(), vueDevTools({ componentInspector: false })],
    define: {
      __API_BASE__: JSON.stringify(env.VITE_API_BASE_URL ?? ''),
    },
    resolve: {
      alias: {
        '@': fileURLToPath(new URL('./src', import.meta.url)),
      },
    },
    ssgOptions: {
      script: 'async',
      formatting: 'minify',
    },
    server: {
      host: '0.0.0.0',
      allowedHosts: ['blip-ambition-rumor.ngrok-free.dev'],
      proxy: {
        '/api': {
          target: 'http://localhost:3000',
          changeOrigin: true,
        },
      },
    },
  };
});
```

- [ ] **Step 2: Commit**

```bash
git add frontend/vite.config.js
git commit -m "feat(seo): add ssgOptions to vite config"
```

---

### Task 4: Crear composable `useSEO.js`

**Files:**
- Create: `frontend/src/composables/useSEO.js`

- [ ] **Step 1: Crear el archivo**

Crear `frontend/src/composables/useSEO.js` con:

```js
import { useHead } from '@vueuse/head'

export function useSEO({
  title,
  description,
  url = 'https://www.finnova.com.mx',
  image = 'https://www.finnova.com.mx/og-image.jpg',
  locale = 'es_MX',
}) {
  useHead({
    title,
    meta: [
      { name: 'description', content: description },
      { name: 'robots', content: 'index, follow' },
      { name: 'author', content: 'Finnova' },
      { property: 'og:type', content: 'website' },
      { property: 'og:title', content: title },
      { property: 'og:description', content: description },
      { property: 'og:url', content: url },
      { property: 'og:image', content: image },
      { property: 'og:locale', content: locale },
      { property: 'og:site_name', content: 'Finnova' },
      { name: 'twitter:card', content: 'summary_large_image' },
      { name: 'twitter:title', content: title },
      { name: 'twitter:description', content: description },
      { name: 'twitter:image', content: image },
    ],
    link: [
      { rel: 'canonical', href: url },
    ],
  })
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/composables/useSEO.js
git commit -m "feat(seo): add useSEO composable with OG and Twitter Card"
```

---

### Task 5: Reescribir `main.js` con ViteSSG

**Files:**
- Modify: `frontend/src/main.js`

- [ ] **Step 1: Reemplazar el contenido completo**

Reemplazar el contenido de `frontend/src/main.js` con:

```js
import { ViteSSG } from 'vite-ssg'
import { createHead } from '@vueuse/head'
import { createI18n } from 'vue-i18n'
import { library } from '@fortawesome/fontawesome-svg-core'
import { FontAwesomeIcon } from '@fortawesome/vue-fontawesome'
import { faApple, faGooglePlay } from '@fortawesome/free-brands-svg-icons'
import en from './i18n/en.json'
import es from './i18n/es.json'
import App from './App.vue'
import './styles.css'

library.add(faApple, faGooglePlay)

export const createApp = ViteSSG(
  App,
  { routes: [{ path: '/' }] },
  ({ app }) => {
    const browserLang =
      typeof navigator !== 'undefined'
        ? navigator.language || (navigator.languages && navigator.languages[0]) || 'en'
        : 'en'
    const locale = browserLang.toLowerCase().startsWith('es') ? 'es' : 'en'

    const i18n = createI18n({
      legacy: false,
      locale,
      fallbackLocale: 'en',
      messages: { en, es },
    })

    const head = createHead()

    app.use(i18n)
    app.use(head)
    app.component('font-awesome-icon', FontAwesomeIcon)
  }
)
```

> El archivo `src/i18n/index.ts` queda intacto pero ya no se importa desde main.js. Usaba `navigator` a nivel de módulo, lo cual rompe SSG.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/main.js
git commit -m "feat(seo): migrate to ViteSSG with inline i18n and head setup"
```

---

### Task 6: Actualizar `App.vue` para usar `useSEO`

**Files:**
- Modify: `frontend/src/App.vue`

- [ ] **Step 1: Agregar el import y llamada a `useSEO` en el `<script setup>`**

En `frontend/src/App.vue`, reemplazar el bloque `<script setup>` con:

```js
<script setup>
import { useSEO } from './composables/useSEO.js'
import Header from './components/Header.vue'
import Hero from './components/Hero.vue'
import HowItWorks from './components/HowItWorks.vue'
import Investment from './components/Investment.vue'
import Footer from './components/Footer.vue'
import Features from '@/components/Features.vue'
import Mail from '@/components/Mail.vue'

useSEO({
  title: 'Finnova | App de Finanzas Personales en México',
  description:
    'Controla tus gastos, ahorra e invierte con Finnova. Diagnósticos financieros, reportes y plan personalizado. Únete al acceso anticipado.',
  url: 'https://www.finnova.com.mx',
})
</script>
```

El bloque `<template>` no cambia.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/App.vue
git commit -m "feat(seo): add useSEO call in App.vue with title and description"
```

---

### Task 7: Actualizar `index.html`

**Files:**
- Modify: `frontend/index.html`

- [ ] **Step 1: Reemplazar el contenido completo**

Reemplazar el contenido de `frontend/index.html` con:

```html
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" href="/favicon-light.ico" media="(prefers-color-scheme: light)" />
    <link rel="icon" href="/favicon-dark.ico" media="(prefers-color-scheme: dark)" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#0FD985" />

    <!-- Hreflang para i18n (es/en) -->
    <link rel="alternate" hreflang="es" href="https://www.finnova.com.mx/" />
    <link rel="alternate" hreflang="en" href="https://www.finnova.com.mx/" />
    <link rel="alternate" hreflang="x-default" href="https://www.finnova.com.mx/" />
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

> `<title>`, `<meta description>` y Open Graph los inyecta `@vueuse/head` automáticamente en build time — no van aquí.

- [ ] **Step 2: Commit**

```bash
git add frontend/index.html
git commit -m "feat(seo): add lang, theme-color and hreflang to index.html"
```

---

### Task 8: Crear `robots.txt` y `sitemap.xml`

**Files:**
- Create: `frontend/public/robots.txt`
- Create: `frontend/public/sitemap.xml`

- [ ] **Step 1: Crear `robots.txt`**

Crear `frontend/public/robots.txt` con:

```
User-agent: *
Allow: /

Sitemap: https://www.finnova.com.mx/sitemap.xml
```

- [ ] **Step 2: Crear `sitemap.xml`**

Crear `frontend/public/sitemap.xml` con:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  <url>
    <loc>https://www.finnova.com.mx/</loc>
    <xhtml:link rel="alternate" hreflang="es" href="https://www.finnova.com.mx/"/>
    <xhtml:link rel="alternate" hreflang="en" href="https://www.finnova.com.mx/"/>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
    <lastmod>2026-04-21</lastmod>
  </url>
</urlset>
```

- [ ] **Step 3: Commit**

```bash
git add frontend/public/robots.txt frontend/public/sitemap.xml
git commit -m "feat(seo): add robots.txt and sitemap.xml"
```

---

### Task 9: Build y verificación final

**Files:** ninguno — solo verificación

- [ ] **Step 1: Ejecutar el build desde `frontend/`**

```bash
cd frontend
npm run build
```

Salida esperada: termina sin errores. Se genera la carpeta `dist/`.

- [ ] **Step 2: Verificar que `dist/index.html` tiene HTML real**

```bash
head -50 dist/index.html
```

El output debe contener texto visible del sitio (headings, párrafos) — **no** solo `<div id="app"></div>`. Si ves el contenido de Hero, Header, etc., el SSG funcionó correctamente.

- [ ] **Step 3: Verificar meta tags en el HTML generado**

```bash
grep -i "finnova\|description\|og:title" dist/index.html
```

Salida esperada: líneas con el título `Finnova | App de Finanzas Personales en México` y la descripción SEO.

- [ ] **Step 4: Commit final**

```bash
git add -A
git commit -m "feat(seo): SSG + SEO implementation complete"
```
